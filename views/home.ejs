<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>City Problems</title>
    <link rel="stylesheet" href="/styles.css"> <!-- Include your CSS file -->
    <script src="https://cdn.tailwindcss.com"></script> <!-- Tailwind for styling -->
</head>
<body class="bg-gray-100">

    <div class="container mx-auto p-6">
        <h1 class="text-3xl font-bold text-center mb-6">City Problems</h1>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            <% problems.forEach(problem => { %>
                <div class="bg-white rounded-lg shadow-md p-4">
                    <h2 class="text-xl font-semibold text-blue-600 hover:underline">
                        <a href="/problems/<%= problem._id %>"><%= problem.title %></a>
                    </h2>
                    <p class="text-gray-700 mt-2"><strong>Posted by:</strong> <%= problem.postedBy.name %></p>
                    <p class="text-gray-500 text-sm"><strong>Date:</strong> <%= new Date(problem.createdAt).toDateString() %></p>
                </div>
            <% }); %>
        </div>
    </div>

</body>
</html>
