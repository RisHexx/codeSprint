<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Problem Details</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* Global button styling */
        .btn {
            font-family: 'Arial', sans-serif;
            font-weight: bold;
            text-transform: uppercase;
            border-radius: 8px;
            padding: 12px 25px;
            font-size: 1rem;
            transition: background 0.3s ease;
        }

        /* Navbar styling */
        .navbar {
            background-color: #ffffff;
            border-bottom: 1px solid #e0e4e7;
        }

        .navbar-brand {
            font-size: 1.5rem;
            color: #3e4e5e;
            font-weight: bold;
        }

        /* Right-aligned buttons styling */
        .ms-auto .btn {
            margin-left: 10px;
            background: #5c89b4;
            border: none;
            color: white;
        }

        .ms-auto .btn:hover {
            background: #3c5b7d;
        }

        /* Main container styling */
        .container {
            max-width: 90%;
            padding: 60px;
            height: 66vh;
            background: #ffffff;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
            border: 1px solid #e0e4e7;
        }

        h2 {
            font-size: 2rem;
            color: #3e4e5e;
        }

        p {
            font-size: 1.1rem;
            color: #4a5c66;
        }

        /* Button at the bottom of the page */
        .btn-bottom {
            position: fixed;
            bottom: 20px;
            left: 20px;
            background: #5c89b4;
            color: #fff;
            border: none;
            padding: 15px 25px;
            font-size: 18px;
            border-radius: 8px;
            cursor: pointer;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .btn-bottom:hover {
            background: #3c5b7d;
        }

        /* Responsiveness for smaller screens */
        @media (max-width: 768px) {
            .navbar {
                padding: 15px;
            }

            .container {
                padding: 30px;
                height: auto;
            }
        }
    </style>
</head>
<body>

    <!-- Navbar -->
    <nav class="navbar bg-body-tertiary">
        <div class="container-fluid">
            <span class="navbar-brand mb-0 h1">Navbar</span>

            <!-- Right-Aligned Buttons -->
            <div class="ms-auto">
                <button class="btn">Complain</button>
                <button class="btn">Sign In</button>
            </div>
        </div>
    </nav>

    <!-- Main Container -->
    <div class="container mt-4">
        <!-- Body content -->
        <h2>Problem Details</h2>
        <p>This is the content that will be inside the container. You can add your text, images, or other elements here as needed.</p>
    </div>

    <!-- Close Button at the bottom of the page -->
    <button class="btn btn-bottom">Close</button>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
